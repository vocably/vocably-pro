version: 2
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - run: echo "Building building building!"
      - run: echo 'export PIU="miu"' >> $BASH_ENV
      - run: echo $PIU
