{
  "name": "@vocably/extension",
  "version": "1.0.0",
  "description": "translation-extension",
  "scripts": {
    "build": "webpack --env production",
    "prebuild": "rimraf ./dist",
    "build-all": "webpack --env production",
    "prebuild-all": "yarn --cwd ../extension-content-script build-all",
    "start": "webpack --watch --env development"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sneas/vocably.git"
  },
  "dependencies": {
    "aws-cognito-chrome-extension": "0.0.0",
    "@vocably/extension-content-script": "^0.0.1",
    "@vocably/extension-service-worker": "^0.0.0"
  },
  "devDependencies": {}
}
